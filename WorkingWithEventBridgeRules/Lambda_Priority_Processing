def lambda_handler(event, context):
    print("This is a high-value order which will receive Priority processing")
    print(f"Order details: {event}")
    return {
        "statusCode": 200,
        "body": "Priority processing initiated for high-value order",
    }
